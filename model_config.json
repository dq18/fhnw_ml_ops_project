{
  "_comment": "Model configuration — edit this to experiment with different models and hyperparameters.",
  "classifier": {
    "type": "ExtraTreesClassifier",
    "params": {
      "n_estimators": 100,
      "class_weight": "balanced",
      "random_state": 42
    }
  },
  "preprocessing": {
    "cat_features": ["rocks", "rain_exposure", "sun_exposure"],
    "drop_cols": ["date", "crag_id"]
  }
}
